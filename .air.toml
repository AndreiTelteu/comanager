root = "."

tmp_dir = "tmp"

[build]
  cmd = "go build -buildvcs=false -o ./tmp/server ./cmd/server"
  bin = "tmp/server"
  include_ext = ["go"]
  exclude_dir = ["frontend", "tmp", "vendor", "internal/embed/frontend/dist"]
  delay = 1000

[log]
  time = true
